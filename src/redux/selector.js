export function getUserData(store) {
    return store.userData;
}

export function getAllServers(store) {
    return store.allServers;
}

export function getAllTracks(store) {
    return store.allTracks;
}